%h1 Editando examen

= render 'form'

= link_to 'Atrás', {:controller => 'admin_examenes'}, :class => 'btn_atras'

:javascript
	$( ".btn" ).button();


	$( ".btn_atras").button({
		icons: {
		primary: "ui-icon-carat-1-w"
		}
	});
